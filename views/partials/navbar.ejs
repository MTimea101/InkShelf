<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Your favorite online library</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav">

        <li class="nav-item">
          <a class="nav-link" href="/">Books</a>
        </li>
        <% if (user && (user.role === 'admin' || user.role === 'curator')) { %>
          <li class="nav-item">
            <a class="nav-link" href="/add-book">➕ Add book</a>
          </li>
        <% } %>
        

        <% if (user) { %>
          <ul class="navbar-nav ms-auto">
            <!-- my borrows -->
            <li class="nav-item">
              <a class="nav-link" href="/my-borrows">Borrowed</a>
            </li>
            
            <li class="nav-item">
              <a class="nav-link" href="/wishlist">Wishlist</a>
            </li>

            <!-- if admin -->
            <% if (user.role === 'admin') { %>
              <li class="nav-item">
                <a class="nav-link" href="/admin">🛠️ Admin</a>
              </li>
            <% } %>
            
            <!-- profile -->
            <li class="nav-item">
              <a class="nav-link fw-bold" href="/profile">
                👤 <%= user.name %>
              </a>
            </li>
            
            <!-- logout -->
            <li class="nav-item">
              <a class="nav-link text-danger" href="/logout">Logout</a>
            </li>
          </ul>
        <% } else { %>
          <ul class="navbar-nav ms-auto">
            <li class="nav-item">
              <a class="nav-link" href="/login">🔑 Login</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/register">📝 Sign in</a>
            </li>
          </ul>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
